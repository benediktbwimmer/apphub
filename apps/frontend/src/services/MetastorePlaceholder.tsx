import ServicesPlaceholder from './ServicesPlaceholder';

export default function MetastorePlaceholder() {
  return (
    <ServicesPlaceholder
      title="Metastore Explorer"
      description="Browse and manage metastore records from this page soon. For now, continue using API clients to query namespaces and audit changes."
    />
  );
}
