name: apphub-demo

x-build-config: &build-config
  context: ..
  dockerfile: docker/Dockerfile.services
  args:
    VITE_API_BASE_URL: ${APPHUB_FRONTEND_API_BASE_URL:-http://localhost:4000}
    VITE_DEMO_OPERATOR_TOKEN: ${APPHUB_DEMO_VIEWER_TOKEN:-demo-viewer-token}
    VITE_STREAMING_CONSOLE_URL: ${APPHUB_REDPANDA_CONSOLE_URL:-http://localhost:28000}
    VITE_MINIO_CONSOLE_URL: ${APPHUB_MINIO_CONSOLE_URL:-http://localhost:9401}

x-shared-env: &shared-env
  NODE_ENV: production
  HOST: 0.0.0.0
  APPHUB_SESSION_SECRET: ${APPHUB_SESSION_SECRET:-change-me}
  APPHUB_ALLOW_INLINE_MODE: "false"
  APPHUB_EVENTS_MODE: redis
  APPHUB_BUILD_EXECUTION_MODE: stub
  APPHUB_LAUNCH_EXECUTION_MODE: stub
  APPHUB_METRICS_ENABLED: "true"
  APPHUB_SCRATCH_ROOT: /var/lib/apphub/scratch
  APPHUB_FILESTORE_BASE_URL: http://filestore:4300
  APPHUB_METASTORE_BASE_URL: http://metastore:4100
  APPHUB_TIMESTORE_BASE_URL: http://timestore:4200
  APPHUB_STREAMING_ENABLED: ${APPHUB_STREAMING_ENABLED:-true}
  APPHUB_STREAM_BROKER_URL: redpanda:9092
  APPHUB_STREAM_MIRROR_WORKFLOW_RUNS: ${APPHUB_STREAM_MIRROR_WORKFLOW_RUNS:-true}
  APPHUB_STREAM_MIRROR_WORKFLOW_EVENTS: ${APPHUB_STREAM_MIRROR_WORKFLOW_EVENTS:-true}
  APPHUB_STREAM_MIRROR_JOB_RUNS: ${APPHUB_STREAM_MIRROR_JOB_RUNS:-true}
  APPHUB_STREAM_MIRROR_INGESTION: ${APPHUB_STREAM_MIRROR_INGESTION:-true}
  APPHUB_STREAM_MIRROR_CORE_EVENTS: ${APPHUB_STREAM_MIRROR_CORE_EVENTS:-true}
  APPHUB_STREAM_TOPIC_WORKFLOW_RUNS: ${APPHUB_STREAM_TOPIC_WORKFLOW_RUNS:-apphub.workflows.runs}
  APPHUB_STREAM_TOPIC_WORKFLOW_EVENTS: ${APPHUB_STREAM_TOPIC_WORKFLOW_EVENTS:-apphub.workflows.events}
  APPHUB_STREAM_TOPIC_JOB_RUNS: ${APPHUB_STREAM_TOPIC_JOB_RUNS:-apphub.jobs.runs}
  APPHUB_STREAM_TOPIC_INGESTION: ${APPHUB_STREAM_TOPIC_INGESTION:-apphub.ingestion.telemetry}
  APPHUB_STREAM_TOPIC_CORE_EVENTS: ${APPHUB_STREAM_TOPIC_CORE_EVENTS:-apphub.core.events}
  APPHUB_FLINK_REST_URL: ${APPHUB_FLINK_REST_URL:-http://flink-jobmanager:8081}
  APPHUB_BUNDLE_STORAGE_BACKEND: s3
  APPHUB_BUNDLE_STORAGE_BUCKET: apphub-job-bundles
  APPHUB_BUNDLE_STORAGE_ENDPOINT: http://minio:9000
  APPHUB_BUNDLE_STORAGE_REGION: us-east-1
  APPHUB_BUNDLE_STORAGE_FORCE_PATH_STYLE: "true"
  APPHUB_BUNDLE_STORAGE_ACCESS_KEY_ID: ${MINIO_ROOT_USER:-apphub}
  APPHUB_BUNDLE_STORAGE_SECRET_ACCESS_KEY: ${MINIO_ROOT_PASSWORD:-apphub123}
  APPHUB_JOB_BUNDLE_STORAGE_BACKEND: s3
  APPHUB_JOB_BUNDLE_S3_BUCKET: apphub-job-bundles
  APPHUB_JOB_BUNDLE_S3_ENDPOINT: http://minio:9000
  APPHUB_JOB_BUNDLE_S3_REGION: us-east-1
  APPHUB_JOB_BUNDLE_S3_FORCE_PATH_STYLE: "true"
  APPHUB_JOB_BUNDLE_S3_ACCESS_KEY_ID: ${MINIO_ROOT_USER:-apphub}
  APPHUB_JOB_BUNDLE_S3_SECRET_ACCESS_KEY: ${MINIO_ROOT_PASSWORD:-apphub123}
  TIMESTORE_STORAGE_DRIVER: s3
  TIMESTORE_S3_BUCKET: apphub-timestore
  TIMESTORE_S3_ENDPOINT: http://minio:9000
  TIMESTORE_S3_REGION: us-east-1
  TIMESTORE_S3_FORCE_PATH_STYLE: "true"
  TIMESTORE_S3_ACCESS_KEY_ID: ${MINIO_ROOT_USER:-apphub}
  TIMESTORE_S3_SECRET_ACCESS_KEY: ${MINIO_ROOT_PASSWORD:-apphub123}
  TIMESTORE_STREAMING_BUFFER_ENABLED: ${TIMESTORE_STREAMING_BUFFER_ENABLED:-true}
  TIMESTORE_STREAMING_BUFFER_RETENTION_SECONDS: ${TIMESTORE_STREAMING_BUFFER_RETENTION_SECONDS:-300}
  TIMESTORE_STREAMING_BATCHERS: ${TIMESTORE_STREAMING_BATCHERS:-""}
  TIMESTORE_CLICKHOUSE_HOST: ${TIMESTORE_CLICKHOUSE_HOST:-clickhouse}
  TIMESTORE_CLICKHOUSE_HTTP_PORT: ${TIMESTORE_CLICKHOUSE_HTTP_PORT:-8123}
  TIMESTORE_CLICKHOUSE_NATIVE_PORT: ${TIMESTORE_CLICKHOUSE_NATIVE_PORT:-9000}
  TIMESTORE_CLICKHOUSE_USER: ${TIMESTORE_CLICKHOUSE_USER:-apphub}
  TIMESTORE_CLICKHOUSE_PASSWORD: ${TIMESTORE_CLICKHOUSE_PASSWORD:-apphub}
  TIMESTORE_CLICKHOUSE_DATABASE: ${TIMESTORE_CLICKHOUSE_DATABASE:-apphub}
  TIMESTORE_CLICKHOUSE_SECURE: ${TIMESTORE_CLICKHOUSE_SECURE:-false}
  AWS_ACCESS_KEY_ID: ${MINIO_ROOT_USER:-apphub}
  AWS_SECRET_ACCESS_KEY: ${MINIO_ROOT_PASSWORD:-apphub123}
  AWS_REGION: us-east-1
  SERVICE_REGISTRY_TOKEN: ${APPHUB_DEMO_ADMIN_TOKEN:-demo-admin-token}
  PGHOST: postgres
  PGPORT: "5432"
  PGUSER: apphub
  PGPASSWORD: apphub
  DATABASE_URL: postgres://apphub:apphub@postgres:5432/apphub
  FILESTORE_DATABASE_URL: postgres://apphub:apphub@postgres:5432/apphub
  TIMESTORE_DATABASE_URL: postgres://apphub:apphub@postgres:5432/apphub
  APPHUB_EVENT_SEQUENCE_DATABASE_URL: postgres://sequencer:sequencer@sequencer-db:5432/apphub_sequences
  APPHUB_EVENT_SEQUENCE_NAME: ${APPHUB_EVENT_SEQUENCE_NAME:-apphub_event_ingress_seq}
  REDIS_URL: redis://redis:6379
  APPHUB_DEV_REDIS_URL: redis://redis:6379
  FILESTORE_REDIS_URL: redis://redis:6379
  METASTORE_REDIS_URL: redis://redis:6379
  TIMESTORE_REDIS_URL: redis://redis:6379
  OBSERVATORY_FILESTORE_BASE_URL: http://filestore:4300
  OBSERVATORY_FILESTORE_TOKEN: ${APPHUB_DEMO_SERVICE_TOKEN:-demo-service-token}
  OBSERVATORY_CORE_BASE_URL: http://core-api:4000
  OBSERVATORY_CORE_TOKEN: ${APPHUB_DEMO_SERVICE_TOKEN:-demo-service-token}
  OBSERVATORY_TIMESTORE_BASE_URL: http://timestore:4200
  OBSERVATORY_TIMESTORE_TOKEN: ${APPHUB_DEMO_SERVICE_TOKEN:-demo-service-token}
  OBSERVATORY_METASTORE_BASE_URL: http://metastore:4100
  OBSERVATORY_METASTORE_TOKEN: ${APPHUB_DEMO_SERVICE_TOKEN:-demo-service-token}
  OBSERVATORY_DATA_ROOT: /var/lib/apphub/scratch/observatory
  OBSERVATORY_CONFIG_OUTPUT: /var/lib/apphub/scratch/observatory/config/observatory-config.json
  OBSERVATORY_DASHBOARD_AUTO_AGGREGATE: ${APPHUB_OBSERVATORY_DASHBOARD_AUTO_AGGREGATE:-false}
  OBSERVATORY_FILESTORE_BACKEND_KEY: observatory-event-driven-s3
  OBSERVATORY_FILESTORE_S3_BUCKET: apphub-filestore
  OBSERVATORY_FILESTORE_S3_ENDPOINT: http://minio:9000
  OBSERVATORY_FILESTORE_S3_REGION: us-east-1
  OBSERVATORY_FILESTORE_S3_FORCE_PATH_STYLE: "true"
  OBSERVATORY_FILESTORE_S3_ACCESS_KEY_ID: ${MINIO_ROOT_USER:-apphub}
  OBSERVATORY_FILESTORE_S3_SECRET_ACCESS_KEY: ${MINIO_ROOT_PASSWORD:-apphub123}
  OBSERVATORY_TIMESTORE_DATASET_SLUG: observatory-timeseries
  OBSERVATORY_TIMESTORE_DATASET_NAME: "Observatory Time Series"
  OBSERVATORY_TIMESTORE_TABLE_NAME: observations
  OBSERVATORY_DASHBOARD_LOOKBACK_MINUTES: "720"
  OBSERVATORY_INSTRUMENT_COUNT: "3"
  AWS_SDK_LOAD_CONFIG: "1"
  APPHUB_OPERATOR_TOKENS: >-
    [{"subject":"demo-admin","token":"${APPHUB_DEMO_ADMIN_TOKEN:-demo-admin-token}","scopes":"*","kind":"user"},{"subject":"demo-service","token":"${APPHUB_DEMO_SERVICE_TOKEN:-demo-service-token}","scopes":["jobs:write","jobs:run","jobs:read","job-bundles:write","job-bundles:read","workflows:write","workflows:run","workflows:read","filestore:read","filestore:write","filestore:admin","metastore:read","metastore:write","metastore:delete","metastore:admin","timestore:read","timestore:write","timestore:admin","timestore:metrics","timestore:sql:read","timestore:sql:exec","observatory:read","observatory:write","observatory:reprocess","runtime:write"],"kind":"service"},{"subject":"demo-viewer","token":"${APPHUB_DEMO_VIEWER_TOKEN:-demo-viewer-token}","scopes":["jobs:read","jobs:run","job-bundles:read","workflows:read","workflows:run","filestore:read","metastore:read","timestore:read","timestore:sql:read","timestore:metrics","observatory:read"],"kind":"user"}]

services:
  sequencer-db:
    image: postgres:15
    restart: unless-stopped
    environment:
      POSTGRES_DB: apphub_sequences
      POSTGRES_USER: sequencer
      POSTGRES_PASSWORD: sequencer
    command:
      - postgres
      - -c
      - synchronous_commit=off
      - -c
      - max_connections=200
      - -c
      - shared_buffers=64MB
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sequencer -d apphub_sequences"]
      interval: 10s
      timeout: 5s
      retries: 12
    volumes:
      - type: bind
        source: ${APPHUB_DATA_ROOT:-./demo-data}/sequencer
        target: /var/lib/postgresql/data
      - type: bind
        source: ./init/sequencer.sql
        target: /docker-entrypoint-initdb.d/00-sequencer.sql

  redpanda:
    image: docker.redpanda.com/redpandadata/redpanda:v24.2.3
    restart: unless-stopped
    command:
      - redpanda
      - start
      - --overprovisioned
      - --smp
      - "1"
      - --memory
      - 1G
      - --reserve-memory
      - 0M
      - --check=false
      - --node-id
      - "0"
      - --kafka-addr
      - PLAINTEXT://0.0.0.0:9092
      - --advertise-kafka-addr
      - PLAINTEXT://redpanda:9092
      - --rpc-addr
      - 0.0.0.0:33145
      - --advertise-rpc-addr
      - redpanda:33145
      - --pandaproxy-addr
      - 0.0.0.0:8082
      - --advertise-pandaproxy-addr
      - redpanda:8082
    healthcheck:
      test: ["CMD", "rpk", "cluster", "info"]
      interval: 10s
      timeout: 5s
      retries: 12
    ports:
      - ${APPHUB_REDPANDA_PORT:-19092}:9092
      - ${APPHUB_REDPANDA_ADMIN_PORT:-19644}:9644
    volumes:
      - type: bind
        source: ${APPHUB_DATA_ROOT:-./demo-data}/redpanda
        target: /var/lib/redpanda/data

  redpanda-init:
    image: docker.redpanda.com/redpandadata/redpanda:v24.2.3
    depends_on:
      redpanda:
        condition: service_healthy
    entrypoint: ["/bin/bash", "-c"]
    command: |
      set -euo pipefail
      sleep 2
      rpk cluster info --brokers redpanda:9092 >/dev/null 2>&1
      for topic in apphub.core.events apphub.ingestion.telemetry apphub.workflows.events apphub.workflows.runs apphub.jobs.runs apphub.streaming.input apphub.streaming.aggregates; do
        rpk topic create "$$topic" --brokers redpanda:9092 --partitions 6 --replicas 1 --config retention.ms=604800000 >/dev/null 2>&1 || \
        rpk topic describe "$$topic" --brokers redpanda:9092 >/dev/null 2>&1
      done
    restart: "no"

  redpanda-console:
    image: docker.redpanda.com/redpandadata/console:v2.6.1
    restart: unless-stopped
    depends_on:
      redpanda:
        condition: service_healthy
    environment:
      KAFKA_BROKERS: redpanda:9092
      KAFKA_TLS_ENABLED: "false"
      SERVER_LISTEN_ADDRESS: 0.0.0.0:8080
      CONSOLE_BASEPATH: /
    ports:
      - ${APPHUB_REDPANDA_CONSOLE_PORT:-28000}:8080

  streaming-seed:
    build:
      <<: *build-config
      target: streaming-runtime
    depends_on:
      redpanda-init:
        condition: service_completed_successfully
    environment:
      APPHUB_STREAM_BROKER_URL: redpanda:9092
      APPHUB_STREAM_INPUT_TOPIC: apphub.streaming.input
    command: ["run", "seed:sample", "--workspace", "@apphub/streaming"]
    restart: "no"

  streaming-job-submit:
    image: flink:1.19.1-scala_2.12
    depends_on:
      flink-jobmanager:
        condition: service_started
      streaming-seed:
        condition: service_completed_successfully
    environment:
      APPHUB_STREAM_BROKER_URL: redpanda:9092
    volumes:
      - type: bind
        source: ${APPHUB_DATA_ROOT:-./demo-data}/flink/checkpoints
        target: /opt/flink/checkpoints
      - type: bind
        source: ../services/streaming/sample-jobs
        target: /opt/apphub/sample-jobs
        read_only: true
    entrypoint: ["/bin/bash", "-c"]
    command: |
      set -euo pipefail
      sed "s|{{BROKER_BOOTSTRAP_SERVERS}}|$$APPHUB_STREAM_BROKER_URL|g" /opt/apphub/sample-jobs/tumbling-window.sql > /tmp/streaming-job.sql
      ./bin/sql-client.sh -f /tmp/streaming-job.sql
    restart: "no"

  flink-jobmanager:
    image: flink:1.19.1-scala_2.12
    restart: unless-stopped
    depends_on:
      redpanda-init:
        condition: service_completed_successfully
    command: jobmanager
    environment:
      FLINK_PROPERTIES: |
        jobmanager.rpc.address: flink-jobmanager
        taskmanager.numberOfTaskSlots: 2
        parallelism.default: 2
        state.backend: filesystem
        state.checkpoints.dir: file:///opt/flink/checkpoints
        execution.checkpointing.interval: 1 min
    ports:
      - ${APPHUB_FLINK_UI_PORT:-8081}:8081
    volumes:
      - type: bind
        source: ${APPHUB_DATA_ROOT:-./demo-data}/flink/checkpoints
        target: /opt/flink/checkpoints
      - type: bind
        source: ../services/streaming/sample-jobs
        target: /opt/apphub/sample-jobs
        read_only: true

  flink-taskmanager:
    image: flink:1.19.1-scala_2.12
    restart: unless-stopped
    depends_on:
      flink-jobmanager:
        condition: service_started
    environment:
      FLINK_PROPERTIES: |
        jobmanager.rpc.address: flink-jobmanager
        taskmanager.numberOfTaskSlots: 2
        parallelism.default: 2
        state.backend: filesystem
        state.checkpoints.dir: file:///opt/flink/checkpoints
        execution.checkpointing.interval: 1 min
    command: taskmanager
    volumes:
      - type: bind
        source: ${APPHUB_DATA_ROOT:-./demo-data}/flink/checkpoints
        target: /opt/flink/checkpoints

  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: apphub
      POSTGRES_PASSWORD: apphub
      POSTGRES_DB: apphub
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U apphub"]
      interval: 5s
      timeout: 5s
      retries: 12
    volumes:
      - type: bind
        source: ${APPHUB_DATA_ROOT:-./demo-data}/postgres
        target: /var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command:
      - redis-server
      - "--save"
      - ""
      - "--appendonly"
      - "no"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 12
    volumes:
      - type: bind
        source: ${APPHUB_DATA_ROOT:-./demo-data}/redis
        target: /data

  clickhouse:
    image: clickhouse/clickhouse-server:24.11
    restart: unless-stopped
    depends_on:
      minio:
        condition: service_healthy
      apphub-bootstrap:
        condition: service_completed_successfully
    environment:
      CLICKHOUSE_DB: ${TIMESTORE_CLICKHOUSE_DATABASE:-apphub}
      CLICKHOUSE_USER: ${TIMESTORE_CLICKHOUSE_USER:-apphub}
      CLICKHOUSE_PASSWORD: ${TIMESTORE_CLICKHOUSE_PASSWORD:-apphub}
      CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT: "1"
    healthcheck:
      test:
        - CMD-SHELL
        - |
          clickhouse-client --host localhost --port ${TIMESTORE_CLICKHOUSE_NATIVE_PORT:-9000} \
            --user "$$CLICKHOUSE_USER" --password "$$CLICKHOUSE_PASSWORD" --query 'SELECT 1'
      interval: 10s
      timeout: 5s
      retries: 12
    ports:
      - ${APPHUB_CLICKHOUSE_HTTP_PORT:-28123}:8123
      - ${APPHUB_CLICKHOUSE_NATIVE_PORT:-29000}:9000
    volumes:
      - type: bind
        source: ${APPHUB_DATA_ROOT:-./demo-data}/clickhouse
        target: /var/lib/clickhouse
      - type: bind
        source: ./clickhouse/config.d
        target: /etc/clickhouse-server/config.d

  minio:
    image: minio/minio:RELEASE.2024-11-07T00-52-20Z
    restart: unless-stopped
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-apphub}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-apphub123}
    command:
      - server
      - /data
      - "--console-address"
      - ":9001"
    ports:
      - ${APPHUB_MINIO_PORT:-9000}:9000
      - ${APPHUB_MINIO_CONSOLE_PORT:-9001}:9001
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:9000/minio/health/live || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
    volumes:
      - type: bind
        source: ${APPHUB_DATA_ROOT:-./demo-data}/minio
        target: /data

  apphub-bootstrap:
    build:
      <<: *build-config
      target: apphub-bootstrap-runtime
    depends_on:
      minio:
        condition: service_healthy
    environment:
      <<: *shared-env
    restart: "no"
    volumes:
      - type: bind
        source: ${APPHUB_DATA_ROOT:-./demo-data}/scratch
        target: /var/lib/apphub/scratch

  core-api:
    build:
      <<: *build-config
      target: core-runtime
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      sequencer-db:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_healthy
      redpanda-init:
        condition: service_completed_successfully
      apphub-bootstrap:
        condition: service_completed_successfully
    environment:
      <<: *shared-env
      PORT: 4000
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:4000/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 20
    ports:
      - ${APPHUB_CORE_PORT:-4000}:4000
    volumes:
      - type: bind
        source: ${APPHUB_DATA_ROOT:-./demo-data}/scratch
        target: /var/lib/apphub/scratch

  core-ingest:
    build:
      <<: *build-config
      target: core-runtime
    restart: unless-stopped
    depends_on:
      core-api:
        condition: service_healthy
    environment:
      <<: *shared-env
    command: ["node", "services/core/dist/ingestionWorker.js"]
    volumes:
      - type: bind
        source: ${APPHUB_DATA_ROOT:-./demo-data}/scratch
        target: /var/lib/apphub/scratch

  core-workflows:
    build:
      <<: *build-config
      target: core-runtime
    restart: unless-stopped
    depends_on:
      core-api:
        condition: service_healthy
    environment:
      <<: *shared-env
    command: ["node", "services/core/dist/workflowWorker.js"]
    volumes:
      - type: bind
        source: ${APPHUB_DATA_ROOT:-./demo-data}/scratch
        target: /var/lib/apphub/scratch

  core-events:
    build:
      <<: *build-config
      target: core-runtime
    restart: unless-stopped
    depends_on:
      core-api:
        condition: service_healthy
    environment:
      <<: *shared-env
    command: ["node", "services/core/dist/eventIngressWorker.js"]
    volumes:
      - type: bind
        source: ${APPHUB_DATA_ROOT:-./demo-data}/scratch
        target: /var/lib/apphub/scratch

  core-event-triggers:
    build:
      <<: *build-config
      target: core-runtime
    restart: unless-stopped
    depends_on:
      core-api:
        condition: service_healthy
    environment:
      <<: *shared-env
    command: ["node", "services/core/dist/eventTriggerWorker.js"]
    volumes:
      - type: bind
        source: ${APPHUB_DATA_ROOT:-./demo-data}/scratch
        target: /var/lib/apphub/scratch

  core-materializer:
    build:
      <<: *build-config
      target: core-runtime
    restart: unless-stopped
    depends_on:
      core-api:
        condition: service_healthy
    environment:
      <<: *shared-env
    command: ["node", "services/core/dist/assetMaterializerWorker.js"]
    volumes:
      - type: bind
        source: ${APPHUB_DATA_ROOT:-./demo-data}/scratch
        target: /var/lib/apphub/scratch

  core-module-services:
    build:
      <<: *build-config
      target: core-runtime
    restart: unless-stopped
    depends_on:
      core-api:
        condition: service_healthy
    environment:
      <<: *shared-env
      MODULE_SERVICE_HOST: core-module-services
      MODULE_SERVICE_PORT_RANGE: 4310-4399
    command: ["node", "services/core/dist/moduleServices/runner.js"]
    volumes:
      - type: bind
        source: ${APPHUB_DATA_ROOT:-./demo-data}/scratch
        target: /var/lib/apphub/scratch

  metastore:
    build:
      <<: *build-config
      target: metastore-runtime
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      redpanda-init:
        condition: service_completed_successfully
    environment:
      <<: *shared-env
      PORT: 4100
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:4100/healthz || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
    volumes:
      - type: bind
        source: ${APPHUB_DATA_ROOT:-./demo-data}/scratch
        target: /var/lib/apphub/scratch

  filestore:
    build:
      <<: *build-config
      target: filestore-runtime
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_healthy
      redpanda-init:
        condition: service_completed_successfully
      apphub-bootstrap:
        condition: service_completed_successfully
    environment:
      <<: *shared-env
      PORT: 4300
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:4300/healthz || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
    volumes:
      - type: bind
        source: ${APPHUB_DATA_ROOT:-./demo-data}/scratch
        target: /var/lib/apphub/scratch

  timestore:
    build:
      <<: *build-config
      target: timestore-runtime
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_healthy
      redpanda-init:
        condition: service_completed_successfully
      clickhouse:
        condition: service_healthy
      apphub-bootstrap:
        condition: service_completed_successfully
    environment:
      <<: *shared-env
      PORT: 4200
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:4200/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
    volumes:
      - type: bind
        source: ${APPHUB_DATA_ROOT:-./demo-data}/scratch
        target: /var/lib/apphub/scratch

  timestore-ingest:
    build:
      <<: *build-config
      target: timestore-runtime
    restart: unless-stopped
    depends_on:
      timestore:
        condition: service_healthy
      streaming-job-submit:
        condition: service_completed_successfully
      clickhouse:
        condition: service_healthy
    environment:
      <<: *shared-env
    command: ["services/timestore/dist/workers/ingestionWorker.js"]
    volumes:
      - type: bind
        source: ${APPHUB_DATA_ROOT:-./demo-data}/scratch
        target: /var/lib/apphub/scratch

  timestore-partition:
    build:
      <<: *build-config
      target: timestore-runtime
    restart: unless-stopped
    depends_on:
      timestore:
        condition: service_healthy
      clickhouse:
        condition: service_healthy
    environment:
      <<: *shared-env
    command: ["services/timestore/dist/workers/partitionBuildWorker.js"]
    volumes:
      - type: bind
        source: ${APPHUB_DATA_ROOT:-./demo-data}/scratch
        target: /var/lib/apphub/scratch

  demo-bootstrap:
    build:
      <<: *build-config
      target: core-runtime
    depends_on:
      core-api:
        condition: service_healthy
      filestore:
        condition: service_healthy
      metastore:
        condition: service_healthy
      timestore:
        condition: service_healthy
    restart: "no"
    environment:
      <<: *shared-env
      LOG_LEVEL: info
      APPHUB_SCRATCH_PREFIXES: ${APPHUB_SCRATCH_PREFIXES:-/app/modules/observatory:/app/modules/observatory/dist}
      OBSERVATORY_SKIP_BUILD: "1"
      OBSERVATORY_BUILD_CLI: "0"
      OBSERVATORY_CORE_TOKEN: ${APPHUB_DEMO_ADMIN_TOKEN:-demo-admin-token}
      APPHUB_STREAMING_ENABLED: "false"
      APPHUB_STREAM_MIRROR_WORKFLOW_RUNS: "false"
      APPHUB_STREAM_MIRROR_WORKFLOW_EVENTS: "false"
      APPHUB_STREAM_MIRROR_JOB_RUNS: "false"
      APPHUB_STREAM_MIRROR_INGESTION: "false"
      APPHUB_STREAM_MIRROR_CORE_EVENTS: "false"
    command: ["tsx", "modules/observatory/scripts/deploy.ts"]
    volumes:
      - type: bind
        source: ${APPHUB_DATA_ROOT:-./demo-data}/scratch
        target: /var/lib/apphub/scratch

  frontend:
    build:
      <<: *build-config
      target: frontend-runtime
    restart: unless-stopped
    depends_on:
      core-api:
        condition: service_healthy
    ports:
      - ${APPHUB_FRONTEND_PORT:-4173}:80
    command: ["nginx", "-g", "daemon off;"]

volumes: {}
