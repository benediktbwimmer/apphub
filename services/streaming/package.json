{
  "name": "@apphub/streaming",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "submit:sample": "node --enable-source-maps --import tsx src/submitSampleJob.ts",
    "seed:sample": "node --enable-source-maps --import tsx src/seedSampleData.ts"
  },
  "dependencies": {
    "kafkajs": "^2.2.4"
  }
}
