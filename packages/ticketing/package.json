{
  "name": "@apphub/ticketing",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "schemas"
  ],
  "scripts": {
    "build": "npm run generate:schema && tsc -b",
    "generate:schema": "tsx scripts/generateSchemas.ts",
    "test": "tsx --test tests/**/*.test.ts"
  },
  "dependencies": {
    "nanoid": "^3.3.7",
    "yaml": "^2.5.1",
    "zod": "^3.25.76",
    "zod-to-json-schema": "^3.24.6"
  },
  "devDependencies": {
    "tsx": "^4.20.5"
  }
}
